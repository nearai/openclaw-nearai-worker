# NEAR AI Cloud API Key (required)
NEARAI_API_KEY=sk-your-api-key-here

# Slack Configuration (required)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token

# Gateway Token (required, for authentication)
CLAWDBOT_GATEWAY_TOKEN=your-gateway-token

# Optional: Slack signing secret (for HTTP mode)
SLACK_SIGNING_SECRET=your-signing-secret

# Optional: Force config regeneration (set to "1" to regenerate config from template)
# CLAWDBOT_FORCE_CONFIG_REGEN=1
