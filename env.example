# NEAR AI Cloud API Key (required)
NEARAI_API_KEY=sk-your-api-key-here

# Optional: Force config regeneration (set to "1" to regenerate config from template)
# OPENCLAW_FORCE_CONFIG_REGEN=1

# Optional: Gateway bind — "lan" (default, local network) or "loopback" (localhost only).
# See README security section when using "lan".
# OPENCLAW_GATEWAY_BIND=lan

# Optional: SSH public key for key-based login into the container (e.g. contents of ~/.ssh/id_ed25519.pub).
# When set, an SSH server runs on port 2222 inside the container. Connect from host: ssh -p 2222 agent@localhost
# From external network: ssh -p 2222 agent@<host-ip> (ensure host firewall/NAT allows port 2222).
# SSH_PUBKEY="ssh-ed25519 AAAA... your-key"

# Optional: Allow agent user to run sudo without password (e.g. su to root for debugging). Set to "1" to enable.
# ALLOW_AGENT_SUDO=0
