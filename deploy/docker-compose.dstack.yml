services:
  cert-init:
    image: alpine:3
    volumes:
      - cert-data:/etc/letsencrypt
    entrypoint: ["/bin/sh", "-c", "mkdir -p /etc/letsencrypt/live && ln -sfn ${OPENCLAW_DOMAIN} '/etc/letsencrypt/live/*.${OPENCLAW_DOMAIN}'"]
    restart: "no"

  nginx:
    image: ${INGRESS_IMAGE:-openclaw-ingress:local}
    container_name: nginx
    network_mode: host
    environment:
      - OPENCLAW_DOMAIN=${OPENCLAW_DOMAIN}
      - CERTBOT_EMAIL=${CERTBOT_EMAIL}
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN}
    volumes:
      - cert-data:/etc/letsencrypt
      - nginx-dynamic:/data/nginx
    configs:
      - source: openclaw_nginx
        target: /etc/nginx/conf.d/openclaw.conf
    depends_on:
      cert-init:
        condition: service_completed_successfully
    restart: unless-stopped

  compose-api:
    image: ${COMPOSE_API_IMAGE:-openclaw-compose-api:local}
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /var/run/dstack.sock:/var/run/dstack.sock
      - management-data:/app/data
      - bastion-data:/app/data/bastion:ro
      - nginx-dynamic:/data/nginx
      - cert-data:/etc/letsencrypt
    environment:
      - ADMIN_TOKEN=${ADMIN_TOKEN}
      - OPENCLAW_DOMAIN=${OPENCLAW_DOMAIN}
      - OPENCLAW_IMAGE=${OPENCLAW_IMAGE}
      - IRONCLAW_IMAGE=${IRONCLAW_IMAGE:-ironclaw-nearai-worker:local}
      - NGINX_MAP_PATH=/data/nginx/backends.map
      - INGRESS_CONTAINER_NAME=nginx
      - COMPOSE_FILE=/app/docker-compose.worker.yml
      - LISTEN_ADDR=0.0.0.0:8080
      - BACKUP_S3_BUCKET=${BACKUP_S3_BUCKET:-}
      - BACKUP_S3_REGION=${BACKUP_S3_REGION:-}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID:-}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY:-}
      - ENV_OVERRIDE_FILE=/app/data/.env.overrides
      - BASTION_SSH_PUBKEY_PATH=/app/data/bastion/id_ed25519.pub
      - BASTION_SSH_PORT=${BASTION_SSH_PORT:-2222}
      - BASTION_SSH_EXTERNAL_PORT=${BASTION_SSH_EXTERNAL_PORT:-${BASTION_SSH_PORT:-2222}}
      - GOOGLE_OAUTH_CLIENT_ID=${GOOGLE_OAUTH_CLIENT_ID:-}
      - GOOGLE_OAUTH_CLIENT_SECRET=${GOOGLE_OAUTH_CLIENT_SECRET:-}
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/health"]
      interval: 5s
      timeout: 3s
      retries: 60
      start_period: 120s
    restart: unless-stopped
    depends_on:
      - nginx

  ssh-bastion:
    image: ${BASTION_IMAGE:-openclaw-ssh-bastion:local}
    network_mode: host
    volumes:
      - bastion-data:/app/data/bastion
    environment:
      - ADMIN_TOKEN=${ADMIN_TOKEN}
      - BASTION_SSH_PORT=${BASTION_SSH_PORT:-2222}
      - COMPOSE_API_PORT=8080
    restart: unless-stopped
    depends_on:
      compose-api:
        condition: service_healthy

  openclaw-updater:
    image: ${UPDATER_IMAGE:-openclaw-updater:local}
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - management-data:/app/data
      - compose-files:/app/compose-live
      - ${DEPLOY_DIR:-.}:/app/deploy
      - /root/.docker:/root/.docker:ro
    environment:
      - UPDATER_COMPOSE_API_IMAGE=${COMPOSE_API_IMAGE_REPO:-}
      - UPDATER_WORKER_IMAGE=${WORKER_IMAGE_REPO:-}
      - UPDATER_IRONCLAW_IMAGE=${IRONCLAW_IMAGE_REPO:-}
      - UPDATER_INGRESS_IMAGE=${INGRESS_IMAGE_REPO:-}
      - UPDATER_BASTION_IMAGE=${BASTION_IMAGE_REPO:-}
      - UPDATER_SELF_IMAGE=${UPDATER_IMAGE_REPO:-}
      - UPDATER_CHANNEL=${UPDATER_CHANNEL:-latest}
      - UPDATER_POLL_INTERVAL=${UPDATER_POLL_INTERVAL:-300}
      - UPDATER_COMPOSE_FILE=${UPDATER_COMPOSE_FILE:-/app/deploy/docker-compose.dstack.yml}
      - UPDATER_ENV_FILE=${UPDATER_ENV_FILE:-/app/deploy/.env.dstack}
      - UPDATER_BASE_ENV_FILE=${UPDATER_BASE_ENV_FILE:-}
      - UPDATER_COMPOSE_PROJECT=${UPDATER_COMPOSE_PROJECT:-}
      - UPDATER_COSIGN_IDENTITY_REGEXP=${UPDATER_COSIGN_IDENTITY:-}
      - UPDATER_COSIGN_ISSUER=https://token.actions.githubusercontent.com
      - UPDATER_HEALTH_URL=http://127.0.0.1:8080/health
      - UPDATER_HEALTH_TIMEOUT=60
      - UPDATER_STATE_FILE=/app/data/updater-state.json
      - DOCKER_REGISTRY_USER=${DOCKER_REGISTRY_USER:-}
      - DOCKER_REGISTRY_TOKEN=${DOCKER_REGISTRY_TOKEN:-}
    restart: unless-stopped
    depends_on:
      - compose-api

  datadog-agent:
    image: ${DATADOG_IMAGE:-datadog/agent:latest}
    container_name: datadog-agent
    network_mode: host
    environment:
      - DD_API_KEY=${DD_API_KEY}
      - DD_SITE=us3.datadoghq.com
      - DD_ENV=${DD_ENV:-dev}
      - DD_HOSTNAME=${DD_HOSTNAME:-openclaw-management-stack}
      - DD_LOGS_ENABLED=true
      - DD_LOGS_CONFIG_CONTAINER_COLLECT_ALL=true
      - DD_CONTAINER_EXCLUDE_LOGS=name:openclaw-.*-gateway-.*
      - DD_CONTAINER_EXCLUDE=name:openclaw-.*-gateway-.*
      - DD_PROCESS_AGENT_ENABLED=true
      - DD_DOGSTATSD_NON_LOCAL_TRAFFIC=true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /proc/:/host/proc/:ro
      - /sys/fs/cgroup/:/host/sys/fs/cgroup:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
    restart: unless-stopped

configs:
  openclaw_nginx:
    content: |
      map $$host $$openclaw_backend {
          include /data/nginx/backends.map;
      }

      map $$http_upgrade $$connection_upgrade {
          default upgrade;
          ''      close;
      }

      # Extract instance name from OAuth state parameter (format: "instance:nonce").
      # Used by auth.${OPENCLAW_DOMAIN} to route callbacks to the correct instance subdomain.
      map $$arg_state $$oauth_instance {
          ~*^([a-z0-9-]{1,63}):   $$1;
          default                  "";
      }

      server {
          listen 80 default_server;
          listen 443 ssl;
          server_name *.${OPENCLAW_DOMAIN};

          ssl_certificate /etc/letsencrypt/live/${OPENCLAW_DOMAIN}/fullchain.pem;
          ssl_certificate_key /etc/letsencrypt/live/${OPENCLAW_DOMAIN}/privkey.pem;

          default_type application/json;

          location / {
              if ($$openclaw_backend = "") {
                  return 503 '{"error":"instance_not_available","message":"This instance is stopped or does not exist"}';
              }

              proxy_pass $$openclaw_backend;
              proxy_http_version 1.1;
              proxy_set_header Upgrade $$http_upgrade;
              proxy_set_header Connection $$connection_upgrade;
              proxy_set_header Host $$host;
              proxy_set_header X-Real-IP $$remote_addr;
              proxy_set_header X-Forwarded-For $$proxy_add_x_forwarded_for;
              proxy_set_header X-Forwarded-Proto $$scheme;
              proxy_read_timeout 86400s;
              proxy_buffering off;
              proxy_cache off;
          }
      }

      server {
          listen 443 ssl;
          server_name api.${OPENCLAW_DOMAIN};

          ssl_certificate /etc/letsencrypt/live/${OPENCLAW_DOMAIN}/fullchain.pem;
          ssl_certificate_key /etc/letsencrypt/live/${OPENCLAW_DOMAIN}/privkey.pem;

          location / {
              proxy_pass http://127.0.0.1:8080;
              proxy_set_header Host $$host;
              proxy_set_header X-Real-IP $$remote_addr;
              proxy_set_header X-Forwarded-For $$proxy_add_x_forwarded_for;
              proxy_set_header X-Forwarded-Proto $$scheme;
          }
      }

      # OAuth auth proxy: receives callbacks from OAuth providers (e.g. Google)
      # and redirects to the correct instance using the `state` query parameter.
      # State format: "instance:nonce" — the map above extracts the instance name.
      server {
          listen 443 ssl;
          server_name auth.${OPENCLAW_DOMAIN};

          ssl_certificate /etc/letsencrypt/live/${OPENCLAW_DOMAIN}/fullchain.pem;
          ssl_certificate_key /etc/letsencrypt/live/${OPENCLAW_DOMAIN}/privkey.pem;

          default_type application/json;

          location = /oauth/callback {
              if ($$arg_state = "") {
                  return 400 '{"error":"missing_state","message":"OAuth callback missing state parameter"}';
              }
              if ($$oauth_instance = "") {
                  return 400 '{"error":"invalid_state","message":"Could not extract instance name from state parameter"}';
              }
              # Redirect to the instance subdomain, preserving ALL original query params
              # (code, state, error, scope, etc. — IronClaw needs state for CSRF check).
              return 302 https://$$oauth_instance.${OPENCLAW_DOMAIN}/oauth/callback$$is_args$$args;
          }

          location / {
              return 404 '{"error":"not_found","message":"Only /oauth/callback is served here"}';
          }
      }

volumes:
  cert-data:
  nginx-dynamic:
  management-data:
  bastion-data:
  compose-files:
